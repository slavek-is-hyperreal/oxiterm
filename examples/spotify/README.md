# Spotify Control Panel THTML Interface

Ta część aplikacji TUI (`panel.thtml` i `panel_mobile.thtml`) stanowi interfejs użytkownika dla integracji z usługą Spotify OAuth 2.0.

Backend sterujący, obsługa tokenów OAuth oraz komunikacja z API Spotify znajduje się w:
`spotify-app-server/app.py`
