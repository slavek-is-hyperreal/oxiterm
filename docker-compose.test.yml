services:
  test-rust:
    build:
      context: .
      dockerfile: Dockerfile.test
  test-spotify:
    build:
      context: .
      dockerfile: spotify-app-server/Dockerfile.test
  lint-layout:
    image: python:3-slim
    volumes:
      - .:/app
    working_dir: /app
    command: >
      python3 .claude/skills/thtml-tcss-authoring/scripts/lint_layout.py examples/ --strict
